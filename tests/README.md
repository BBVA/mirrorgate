# Mirrorgate Integration Testing

Module for integration tests. Currently they are based in perceptual testing.

## Prerequisites

You need to have installed both docker and docker-compose.

## Execution

Run `./run.sh` from the command line.
